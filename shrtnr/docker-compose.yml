version: '3.9'

services:
  redis:
    image: redis/redis-stack:latest
    ports:
      - 6379:6379
      - 8001:8001
    volumes:
      - ./.redis:/data

  # nextjs:
  #   depends_on:
  #     - prometheus
  #     - redis 
  #   image: cloudflare/takehome
  #   ports:
  #     - 3000:3000
  #   build:
  #     context: ./
  #     dockerfile: Dockerfile

  prometheus:
    image: prom/prometheus:latest
    ports:
      - 9090:9090
    volumes:
      - ./prometheus:/etc/prometheus